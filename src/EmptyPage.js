import React from "react";

const EmptyPage = ({ title }) => {
    return (
        <div>
            <h2>{title}</h2>
            {/* Add any additional content for the empty page here */}
        </div>
    );
};

export default EmptyPage;